-@categories.each do |category|
	%p
		=category.name
		=link_to 'Редактировать', edit_admin_category_path(category)
		=link_to 'Удалить', admin_category_path(category), method: :delete, data: {confirm: 'Уверен?'}